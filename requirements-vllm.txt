# Optional vLLM Dependencies
# Install with: pip install -r requirements-vllm.txt

# Core vLLM
vllm>=0.3.0

# Model serving and OpenAI compatibility
openai>=1.0.0

# GPU acceleration (NVIDIA)
# Note: You may need to install torch separately with CUDA support:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# Tokenization and transformers
transformers>=4.40.0
tokenizers>=0.15.0

# Optional: Quantization support
autoawq>=0.2.0

# Optional: Performance monitoring
pynvml  # NVIDIA GPU monitoring

# Testing with vLLM
pytest-asyncio>=0.23.0
httpx[http2]>=0.25.0
